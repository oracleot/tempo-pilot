// Deprecated shim: the calendar picker screen now lives in
// `features/calendar/picker`. This re-export avoids breaking older imports
// while the feature stabilises. New code should import the picker module
// directly for clarity.
export 'package:tempo_pilot/features/calendar/picker/calendar_picker_screen.dart';
